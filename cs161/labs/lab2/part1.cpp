#include<iostream>
#include<cmath>
#include<string>

using namespace std;

int main( ) {

	double a = pow(2,32) -1;
	cout << a << endl;

	double b = pow(5.0,3);
	cout << b << endl;
	
	double c = sqrt(25);
	cout << c << endl;	
	
	double d = fmod(34.0,5);
	cout << d << endl;

	double e = sqrt(pow(2,64));
	cout << e << endl;

return 0;
}
